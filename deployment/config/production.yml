# Configure Compute - Production Configuration
# Deployed to: configure-compute-editor-production

cloud_run:
  service_name: configure-compute-editor-production
  service_account: sa-configure-compute@fastly-soc.iam.gserviceaccount.com

  resources:
    cpu: "1"
    memory: "512Mi"
    cpu_request: "500m"
    memory_request: "256Mi"

  scaling:
    min_instances: 1  # Keep one instance warm for production
    max_instances: 10
    concurrency: 80
